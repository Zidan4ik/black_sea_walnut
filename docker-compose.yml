services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - /home/ubuntu/uploads:/app/uploads
    environment:
      - TZ=Europe/Prague
      - SPRING_PROFILES_ACTIVE=prod
      - DB_URL=${AWS_DB_URL}
      - DB_USERNAME=${AWS_DB_USERNAME}
      - DB_PASSWORD=${AWS_DB_PASSWORD}